# Screenshot

Inserisci qui due immagini per il README principale:

- `desktop.png` — homepage a schermo intero (viewport ~1280px)
- `mobile.png` — homepage su mobile (viewport ~390px)

Si catturano facilmente dal sito in esecuzione (`npm run dev`) con gli
strumenti per sviluppatori del browser (device toolbar per il mobile).
