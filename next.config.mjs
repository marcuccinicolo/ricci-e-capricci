/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Tutte le immagini sono locali in /public/images/ e ottimizzate da next/image.
};

export default nextConfig;
